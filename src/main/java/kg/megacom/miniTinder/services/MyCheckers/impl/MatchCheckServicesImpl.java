package kg.megacom.miniTinder.services.MyCheckers.impl;

import kg.megacom.miniTinder.models.Acount;
import kg.megacom.miniTinder.models.Orders;
import kg.megacom.miniTinder.services.MyCheckers.MatchCheckServices;
import kg.megacom.miniTinder.services.OrderServices;
import kg.megacom.miniTinder.services.impl.OrderServicesImpl;

public class MatchCheckServicesImpl {
//    OrderServices orderServices = new OrderServicesImpl();


//    @Override
//    public boolean myMatchCheck(Orders orders, Long id, Long id1) {
//        System.out.println(acount.getLogin());
//        if(acount.getLogin().equals(orders.getRecipientId().getLogin()) && orders.getSenderId().equals(id1)){
////            orderServices.findById(id).setMatch(true);
//            return true;
//        }else
////            orderServices.findById(id).setMatch(false);
//        return false;
//    }
}
//orders.getRecipientId().equals(orders.setSenderId(id))&&orders.getSenderId().equals(id1)
